-- ============================================================
-- jj_deposit_reset.sql — ล้างเงินประกันทุกคนเป็น 0 · เริ่มหักจริงงวด ส.ค. 2569
-- ไม่นับย้อนหลังทุกกรณี · กด Ctrl+A เลือกทั้งหมดก่อน Run · รันซ้ำได้
-- ============================================================

-- 1) มีช่องตั้งค่า "เริ่มหักตั้งแต่งวด" แน่นอน (เผื่อยังไม่ได้รัน jj_deposit_start.sql)
ALTER TABLE payroll_settings ADD COLUMN IF NOT EXISTS dep_start text NOT NULL DEFAULT '';

-- 2) เริ่มหักตั้งแต่งวด ส.ค. 2569 (งวดปัจจุบัน) — งวดก่อนหน้าไม่ถูกนับเลย
UPDATE payroll_settings SET dep_start = '2026-08';

-- 3) ยอด "เก็บมาก่อน" ของพนักงานทุกคน = 0
UPDATE employees SET deposit_opening = 0;

-- 4) ลบรายการแก้หักรายงวดที่เคยทดลองไว้ทั้งหมด (ถ้ามี)
DELETE FROM deposit_entries;
