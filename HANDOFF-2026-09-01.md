# JJ P&L — HANDOFF ฉบับ 1 ก.ย. 2569 (ย้ายไป Claude Code)

> ต่อยอดจาก `history/JJ-PnL-HANDOFF-2026-08-22.md` (โครงพื้นฐาน + งานช่วงบันทึกบิล/OCR) — ไฟล์นี้เพิ่ม **สปรินต์ 26 ส.ค.–1 ก.ย.**: เชื่อมระบบนับสต๊อก, เทียบสาขา, แปลงหน่วย, PDF/Excel, 🤖 safety/max β, เอกสารคู่มือ
> กติกาปฏิบัติอยู่ใน `CLAUDE.md`

## 1. ค่าคงที่
| อะไร | ค่า |
|---|---|
| Supabase URL / key | `https://aikyxvluaiubdidqxwnd.supabase.co` · `sb_publishable_Bn6BMtcjasoPT3RZ_ekyOg_SLWWp-nm` (ฝังในไฟล์) |
| เว็บจริง / repo | `https://thananant.github.io/JJ-PnL/jjmk-pnl.html` · `thananant/JJ-PnL` (user วางไฟล์ทับ commit เอง) |
| แอพนับสต๊อก | JJ StockCheck `https://thananant.github.io/jjmk-stock/` — Supabase **โปรเจกต์เดียวกัน** ตาราง `products`, `stock_counts` (ไม่มี prefix; ตาราง `rm_*` ว่าง/ไม่ใช้) |
| id สาขาในระบบนับ | รัชดา `b19f0a17b4472` · ลาดพร้าว `b19f0a17b448212` (`STK_BR`) — ตาราง `branches` เป็นของระบบเงินเดือน **ห้ามใช้** |
| วันเริ่มข้อมูลนับ | `STK_MIN='2026-07-31'` (ก่อนหน้านี้ = ระบบเก่า ทิ้งทั้งหมด) |
| OCR Worker | Cloudflare `jjmk-pnl-ocr` → `https://jjmk-pnl-ocr.thananantpatt.workers.dev` · secret `GEMINI_API_KEY` · โมเดล gemini-3.7-flash → 3.6 → flash-latest |
| Mix888 | โปรเจกต์ Supabase แยก `eqbzpgynzgdwvouuzfwt` (key publishable `sb_publishable_HqLNQDwR4omYcb7BNUEKIw_vyHCo4N-`) แต่**บัญชีเดียวกัน** — โควตา egress ร่วม |
| Deploy | html → GitHub วางทับ · SQL → Supabase SQL Editor รันครั้งเดียว · Worker → Cloudflare Edit code |

## 2. สถาปัตยกรรม/คอนเวนชัน
- ไฟล์เดียว `jjmk-pnl.html` (~435KB, 6 script blocks) · state `S` (S.br สาขา `JJRD|JJLP|JJOF|ALL`, S.m เดือน `YYYY-MM`, S.tab, S.cache) · `LS.get/set` localStorage
- API: `api(method,path,body,prefer)` · `sel/selQ` (quiet) · **`selAll/selAllQ` แบ่งหน้า 1,000 แถว — ใช้กับทุก query ช่วงวันที่** · `ins/upd/del` · `ups(t,rows,'col,col')` = upsert merge-duplicates
- แท็บ `RENDER.dash|income|exp|detail|vatrep|usage|actual|sum|pv|etc|set` · โมดัล `openModal(html)/closeModal()` ใน `#modalWrap > #modalBox` · `toast(msg,isErr)`
- Realtime: `rtStart/rtApply/rtTick` (ตาราง pnl_* + pnl_stock_map ใน publication) — เมื่อ event มาจะ `show(S.tab)` และเรียก `stkBadge()`
- ตัวช่วยตัวเลข: `num, fmt, fmtI, fq (ตัดทศนิยมศูนย์), pctF, thDate, dstr, monthDays, stkAddDays, ocrNorm (normalize ชื่อ), esc`
- สิทธิ์: `pnl_users` + `permOf(view)`; หน้า view-only แสดงการ์ด "ดูอย่างเดียว"

## 3. ฐานข้อมูล (เพิ่มจาก handoff เดิม)
| ตาราง | คอลัมน์สำคัญ | หมายเหตุ |
|---|---|---|
| `pnl_bill_items` | branch, d, supplier_id, item, unit, qty, price, discount, bill_discount, ship_fee, other_fee, sort, bill_no, vat_mode | ดัชนี (branch,d) (branch,supplier_id,d) ใน indexes.sql |
| `pnl_expense_daily` | branch, d, supplier_id, amount, paid … unique(branch,d,supplier_id) | dtSave upsert ยอดบิลต่อซัพ/วัน |
| `pnl_income_daily` | branch, d, sales_pos_am/pm … | ยอดขาย = ตัวหาร "ต่อ ฿10,000" |
| `pnl_sup_items` | supplier_id, item, unit, sort | ชุดสินค้าประจำ (สร้างอัตโนมัติจากบิล) |
| `pnl_item_alias` | supplier_id, alias, item, unit, bill_unit, factor | สมอง OCR |
| **`pnl_stock_map`** | id, branch, product_id (unique), product_name, pnl_item, bill_unit, stock_unit, factor, active | ผูกสินค้านับ↔ชื่อบิล · `active=false` = ข้าม · **product_id `none:<br>:<norm>`** = จำว่า "ไม่มีในระบบนับ" (ห้ามลบตอน unskip → มี `not.like.none:*`) |
| **`pnl_unit_conv`** | item, from_unit, to_unit, factor, unique(item,from_unit) | แปลงหน่วยรายสินค้า ใช้ทุกซัพทุกสาขา |
| `products` (StockCheck) | id, branch_id, name, unit, sup, cat_key, cat_label, sort, safety, max, pack, order_step, deleted_at | เราอ่านทั้งหมด + **เขียนได้เฉพาะ safety,max** (stockwrite.sql) |
| `stock_counts` (StockCheck) | branch_id, branch_name, count_date, product_id, qty, out_of_stock, created_at | 78k+ แถว นับทุกวัน ~17:01 ก่อนเปิด |
- RLS ทุกตาราง pnl_* = policy `allow_all` + anon key → **ความเสี่ยงอันดับ 1** (ดู §8)

## 4. ฟีเจอร์ที่ส่งในสปรินต์นี้ (ชื่อฟังก์ชันไว้ grep)
1. **ใช้จริง (สต๊อก)** แท็บ `actual` — `stkLoad()` (counts/bills/products/map/sales ตั้งแต่ STK_MIN) → `RENDER.actual`: ต่อสินค้า `r.per[]` ช่วงนับ {Pd,Cd,q0,q1,buy,use,sale,p10} · สูตร ใช้จริง = นับก่อน + บิล(P<d≤C)×factor − นับหลัง · ยอดขายคู่ [P,C) · chip เทียบ avg10 ±20%
   - โหมดดู `S.stkView` = `last|day|range|auto` (`stkViewSet`, `stkDayStep`, S.stkDay/stkFrom/stkTo, `S._stkAllD`)
   - แผงผูก **พับเป็นค่าตั้งต้น** (`S._stkBindOpen`, LS `jj_stkbind_open`): datalist ชื่อบิล, กรอง, ติ๊กหลายตัว `stkBindSel`, ผูกกระจกข้ามสาขา `stkMirrorRows`/`stkCopyFromOther`, ข้าม/`stkUnskipAll`
   - แถบ ⏳ เมื่อวันนับล่าสุดไม่มีบิล · ค่าติดลบทาแดง ⚠
   - `stkBadge()` ป้ายแดงบนเมนู + แถบ `#stkDashNote` บนแดชบอร์ด (จำนวนสินค้ายังไม่ผูกของสาขา) — เรียกที่ boot, สลับสาขา/เดือน, หลัง RENDER.actual, rtApply/rtTick, และใน `show()` เมื่อ dash
   - **🤖 β** `stkAutoCalc/stkAutoHtml/stkAutoWrite/stkAutoWrite2`: ใช้เฉลี่ยราย จ–อา (ถัวใช้จริงตามสัดส่วนยอดขายรายวัน) → safety = avg×`S.autoFS`(1.2) · max = safety×`S.autoFM`(2) · วันหยุด ×`S.autoHolF`(1.3) · ปัด `stkRoundSm` (<8 ceil, ≥8 `stkRound05`) · ข้อมูล <2 จุด = "ข้อมูลน้อย" ไม่ติ๊ก · เขียนผ่าน `ups('products',rows,'id')` หลังโมดัลยืนยัน
2. **ลงบิลเจอชื่อใหม่ → ชวนผูก** `dtStkPrompt/stkLinkShow/stkLinkBind/stkLinkNone/stkLinkSkip` (เรียกใน dtSave หลัง toast; `_newN` = รายการที่ไม่อยู่ใน S._dtCat)
3. **แปลงหน่วย** `loadUconv()/ucv(item,unit,qty,price)` ใช้ใน RENDER.usage (g/gi/gc/prev) และ stkLoad (B map) · ป้าย "⚠ หน่วยปน" (`mixSet`, `S._uMix`) → `uconvModal/uconvSave`
4. **หน้าการใช้ของ**: โหมด `S.usageMode` = `item|sup|cmp` (cmp เฉพาะ ALL: `gc` แยกสาขา, `salesBr`, เรียง |฿/หมื่นต่าง|) · ค้นหา `usageFilter` · โมดัล `usageDetailItem/usageDetail` + กราฟ `dtlChart` (แท่งจำนวน + เส้นราคาแยกซัพ) · **PDF** `usagePdf` (picker กรองซัพ/หมวด `usagePdfFilter`, กริดแถว) → `usagePdfGo/usagePdfBuild` (#printArea .ppage; CSS print: @page 11mm + padding 14–15mm ฝังขอบ ~1 นิ้ว เพราะ Safari iOS ไม่อ่าน @page; min/max-height 236mm ล็อก 1 สินค้า/หน้า; ตาราง 22 แถว; กล่องกลาง = ยอดใช้แยกสาขา) · **Excel** `usageXlsx` 4 ชีท
5. **เพดาน 1,000 แถว** แก้ทุก fetch ช่วงเดือนเป็น selAll (vatrep, usage, dtMonthList, income/expense aggregates, ชื่อบิลใน actual)
6. เอกสาร: `docs/JJ-PnL-พรีเซนเทชั่น.pptx` (pptxgenjs, `docs/mk_pptx.js`) · `docs/JJ-PnL-คู่มือการใช้งาน.pdf` (reportlab, `docs/mk_manual.py`, ฟอนต์ Sarabun ใน docs/) · แผนภาพ d1–d3

## 5. SQL ในสปรินต์ (สถานะ)
| ไฟล์ | ทำอะไร | สถานะ |
|---|---|---|
| jjmk_pnl_stockmap.sql | ตาราง pnl_stock_map + realtime | รันแล้ว |
| jjmk_pnl_stockread.sql / stockread2.sql | GRANT SELECT + policy anon อ่าน products/stock_counts | รันแล้ว |
| jjmk_pnl_indexes.sql | ดัชนี + query ขนาด DB | ส่งแล้ว — **ยังไม่ได้เลขขนาด DB กลับมา** |
| jjmk_stock_audit.sql, audit/jjmk_audit_1..4.sql | ตรวจสองสาขา (อ่านอย่างเดียว) | รันแล้ว (audit_3 user ยังไม่ยืนยันว่าว่าง) |
| jjmk_stock_rename.sql | ชื่อสองสาขาให้ตรง 3 ตัว + เติมหน่วยว่าง | รันแล้ว ✅ |
| jjmk_names_export.sql | 3 คอลัมน์ นับ/บิล/สถานะ | รันแล้ว (CSV 209 แถว) |
| jjmk_names_fix2.sql | 8 จุดจากไฟล์เคาะ (แก้ผูกผิด fanta/บะหมี่ · ส้ม/ลิ้นจี่ scope เซ็นซอรี่ · ถุงขยะ 36→35 · เปลี่ยนชื่อฝั่งนับ 5) | ส่งแล้ว — **ไม่มีคำยืนยันว่ารัน** |
| jjmk_names_fix3.sql | รวมอายิโนะโมะโต๊ะ ชื่อ+หน่วย กก. | ส่งแล้ว — ไม่มีคำยืนยัน |
| jjmk_billnames_check.sql | หาคู่แฝดชื่อบิล | รันแล้ว |
| jjmk_pnl_unitconv.sql | ตารางแปลงหน่วย | ส่งแล้ว — ต้องรันก่อนใช้ปุ่มบันทึกอัตราแปลง |
| jjmk_pnl_stockwrite.sql | grant update(safety,max) products | ส่งแล้ว — ต้องรันก่อนใช้ 🤖 β เขียน |
| jjmk_fix_month.sql | ย้ายบิล ก.ย.2–30 → ส.ค. (บิล+expense_daily ต่อเลขบิล) | ส่งแล้ว 1 ก.ย. |
- ไฟล์ทดสอบสคีมา: `sql/local_test_schema.sql` (pg_dump ของ DB ทดสอบ local; ไม่ใช่สคีมาโปรดักชันเป๊ะ)

## 6. วิธีทดสอบ
- `npm i` → `npm test` (รัน `run-tests.sh` = 49 ชุดหลัก) · `npm run check` = parse script blocks
- โครงเทสต์: jsdom โหลด html, ตัด `<script src=cdn>`, ใส่ mock Chart/XLSX, `w.fetch` mock ตาม url.includes(...) · เรียก `w.eval("show('usage')")` แล้ว assert textContent/DOM · ดู `tests/smoke80.js` (ALL/cmp/PDF), `smoke82.js` (แปลงหน่วย+actual), `smoke87.js` (β), `smoke88.js` (Excel) เป็นแม่แบบ
- แพตช์ที่ anchor มี `\\'` (เช่น onclick usageDetailItem): หา index ด้วย `s.find(...)` แล้ว `s[start:end]` แทนพิมพ์ escape เอง
- แพตช์เคยพลาด: `el.innerHTML=\`` ซ้ำ 5 ที่, modal footer ซ้ำ 2 ที่, คำว่า "หมู" อยู่ในชื่อร้าน — ขยาย anchor เสมอ

## 7. งานค้าง / รอ user เคาะ
- **หน่วยนับ 7 ตัวสองสาขาไม่ตรง** (ช็อกโกแลต/นม/มัทฉะ/ชีส/สาหร่ายวากาเมะ/หอยแมลงภู่→ถุง, ฟองเต้าหู้ทอดม้วน→แพ็ค เสนอ "ตามรัชดา") — ชีท 2 ใน `jjmk_ตรวจสองสาขา.xlsx` ยังไม่กลับมา · เปลี่ยนแล้วพนักงาน ลพ.ต้องนับหน่วยใหม่ + safety/max ใหม่
- ยังไม่ผูก ~33 ตัว (ส่วนใหญ่ครัวกลาง/Mix888/คลัง ไม่เคยลงบิล) — user บอก "รอไปเรื่อย ๆ" · ของครัวกลางไม่มีบิล → ใช้จริงติดลบซ้ำ (แนะนำเริ่มลงบิลครัวกลาง)
- ไอศกรีม: รัชดาไม่มีบิลไอศกรีม ส.ค. เลย — ฝากเช็คหน้าร้านว่าบิลตกหล่นไหม
- สูตรร่างสั่งของในแอพนับ (max−คงเหลือ?) user ยังไม่ตอบ → ตัวคูณ max ใน β ตั้ง 2 รอ
- **NAS backup รายคืน** (โครง `NAS/backup/jj-supabase/YYYY-MM-DD/*.csv` ตี 3, รายวัน 30 วัน + รายเดือน) — รอยี่ห้อ/รุ่น NAS
- **Mix888 egress** 4.49/5 GB (Mix888 4.46) เสี่ยงโดนจำกัดทั้งบัญชี — รอวันรีเซ็ต + ภาพ Storage bucket; แผน: ย้ายรูปไป Cloudflare R2 (มีบัญชี CF)
- ผลรัน indexes.sql (ขนาด DB) ยังไม่ส่ง · audit_3 ยังไม่ยืนยันว่าว่าง · fix2/fix3 ยังไม่ยืนยันว่ารัน
- เฟส 2 เต็ม: pg_cron เขียน safety/max เองทุกเช้า (หลัง β ใช้จนไว้ใจ) · เฟส 3: ปริมาณสั่ง = คาดการณ์ถึงรอบส่ง + safety − คงเหลือ ปัดตามแพ็ค → โฟลว์ LINE เดิม
- แผนเก่ายังไม่เริ่ม: บัญชีคู่ + invoice matching + ใบสำคัญรับเงิน

## 8. ความเสี่ยงที่รู้อยู่
1. RLS allow_all + anon key ในไฟล์สาธารณะ → ใครมี URL แก้/ลบข้อมูลผ่าน API ได้ (ล็อกอินเป็นแค่ UI) → ควรย้าย Supabase Auth + RLS รายบทบาท
2. ไม่มีสำเนาข้อมูลเลย (คลาวด์ที่เดียว 100%)
3. โควตา egress ร่วม Mix888
4. ไฟล์เดียว 435KB โตเรื่อย — ยังไหว แต่ควรมี CHANGELOG/version marker ใน repo

## 9. โรดแมปที่ user เห็นชอบ (ลำดับที่เสนอ)
1. ปิดความเสี่ยง (backup NAS, Mix888→R2) 2. LINE สรุปเช้าอัตโนมัติ (pg_cron + LINE Messaging API) 3. 🤖 เต็มรูปแบบ + สั่งของอัตโนมัติผ่านโฟลว์แอพนับ 4. JJ Copilot (Claude API ผ่าน Worker อ่านอย่างเดียว) 5. ตัวจับของหาย (สถิติซอง) 6. ต้นทุนต่อหัวลูกค้า (ต้องได้จำนวนหัวจาก POS) 7. จอทีวีหลังร้าน

## 10. เพิ่ม 2 ก.ย. 2569 (session Claude Code)
- **Excel การใช้ของ** ใช้/วัน จ–พฤ/ศ/ส–อา เปลี่ยนเป็นสูตรถัวตามยอดขาย (เหมือนหน้าจอ/PDF) · คอลัมน์ดิบเปลี่ยนชื่อ "ซื้อ (ตามวันบิล)" · smoke89 · deploy แล้ว
- **P&L รู้จักทั้งสองชื่อ**: `loadStkNames/stkNm/stkNmHtml/billNmHtml` — ป้าย 📦 ชื่อนับ ข้างชื่อบิล (การใช้ของ/บันทึกบิล/โมดัล/PDF/Excel) · หน้าใช้จริงโชว์ 🧾 ชื่อบิล · อ่านจาก **view `pnl_stock_names`** (`sql/jjmk_pnl_stocknames_view.sql` — ยังไม่ยืนยันว่ารัน) ตกกลับ `pnl_stock_map` ถ้าไม่มี view · RT_TABLES เพิ่ม pnl_stock_map · smoke90 · deploy แล้ว (รุ่นก่อน view)
- `sql/jjmk_stockmap_export.sql` + `docs/jj-stock-name-map.md` = ให้ jj-stock อ่านชื่อบิล (จับคู่ด้วย product_id ห้ามเทียบชื่อ)
- ~~แอพสั่งของใหม่ `jjmk-order.html`~~ **ยกเลิกแล้ว** (user ตัดสินใจเชื่อมกับแอพนับเดิม jj-stock แทน) — โค้ด/SQL/เทสต์อยู่ในประวัติ git commit `4d681d7` (main) และ `79e0c25`, `85496`… (branch งาน) ถ้าต้องหยิบกลับ · ถ้าเคยรัน setup แล้วให้รัน `sql/jjmk_order_drop.sql`
- เทสต์รวม 51 smoke · เครื่องเทสต์นี้ jsdom ไม่มี TextEncoder (smoke38 shim แล้ว)

## 11. เพิ่ม 5 ก.ย. 2569 (session Claude Code)
- **ปฏิทินรายรับโชว์ยอดรายวัน** เหมือนรายจ่าย · **เงินโอนคีย์ = ก่อนหัก** โชว์ "เข้าบัญชีจริง" (หัก 0.35%+VAT7% ของค่าธรรมเนียม, `TRF_FEE/trfNet/trfNoteHtml`) · **คงเหลือสุทธิ = netVat** ตั้งจากยอดรวม VAT วงเล็บ VAT รอนำส่ง · แผงเงินโอนรายวัน `trfRepHTML` หน้ารายรับ (calwrap c3) — deploy แล้วถึง commit 86168c9 · smoke38 ครอบหมด
- **แท็บ "ตรวจสอบเงิน" (`recon`)**: อัปโหลดไฟล์รายการจาก Beam (CSV หลายไฟล์/หลายบัญชี) → `beamAgg` ตัดตี 6 (`beamBizday` — รายการก่อน 06:00 = วันขายก่อนหน้า) กันแถวซ้ำด้วย charge_id ธง `complete=false` วันหัว/ท้ายช่วงไฟล์ → upsert `pnl_beam_daily` (acct,d) → ตารางเทียบ "คีย์ในแอพ (ทุกสาขารวม)" vs "Beam ก่อนหัก" รายวัน ✓/✗ + ⏳ PENDING + ค่าธรรมเนียมเดือน · การ์ดล่าง = ตรวจปิดกะรายสาขา (สูตร `shiftDiffCalc` เดิม) · **โหมดรวม 2 สาขา** — user จะแยก merchant ต่อสาขา (สาขาละ 2 บัญชี: acc1 ≤40,000/วัน แล้วตัดเข้า acc2) เมื่อไรจะมาบอก ตอนนั้นค่อยเพิ่ม mapping acct→branch · ต้องรัน `sql/jjmk_pnl_beam.sql` (ยังไม่ยืนยันว่ารัน) · smoke91 · สิทธิ์: เพิ่ม `recon` ใน VIEWS พนักงานเก่าถูกซ่อนอัตโนมัติ (ไม่มี key ใน perms)
- ข้อมูลจริง Beam 1–5 ก.ย.: 2 บัญชี `jingjaimookt` (จำกัด 40k/วันปฏิทิน — POS ยิงเข้าจนใกล้เต็มแล้วสลับเอง ไม่เคยเกิน) + `jingjai` · ค่าธรรมเนียมตรง TRF_FEE เป๊ะ · settle T+1 · แยกสาขาจากไฟล์ไม่ได้ (device/serial ว่าง, reference สุ่ม) · รายการมีเฉพาะ 12:00–02:59 → cutoff ตี 6 ปลอดภัย
- **ไฟล์เงินเคลื่อนไหว Beam (balances)** เพิ่มเข้าแท็บตรวจสอบเงิน: ปุ่มอัปโหลด 2 ช่อง (รายการ/เงินเคลื่อนไหว — `beamUpload` ดูหัวคอลัมน์แยกชนิดเอง สลับช่อง/ปนกันได้) · `beamMoves` เก็บรายแถวลง `pnl_beam_moves` (PK acct,ts,btype; acct มาจาก**ชื่อไฟล์**เพราะไฟล์ไม่มีคอลัมน์ merchant — `beamAcct`) · การ์ด "🏦 เงินเข้าบัญชีธนาคาร" = แถว PAYOUT รายวันแยกบัญชี+รวมเดือน ไว้เทียบสมุดบัญชีธนาคาร + โชว์คงค้างใน Beam + เตือน ADJUSTMENT · โครงสร้างไฟล์: PAYMENT ตี 2 = ก้อนยอดขายเมื่อวานตามปฏิทิน (ยืนยันตรงกับไฟล์รายการเป๊ะ) · PAYOUT ~10-11 โมง = เข้าธนาคารจริง · เสาร์-อาทิตย์ไม่มี payout สะสมเข้าจันทร์ · smoke92 · SQL รวมอยู่ใน `jjmk_pnl_beam.sql` ไฟล์เดิม (รันซ้ำได้)
- **สาขาส่วนกลาง (ออฟฟิศ)**: `pnl_branches.is_central` (`sql/jjmk_pnl_central.sql` — ยังไม่ยืนยันรัน; แอพ degrade ได้ถ้าคอลัมน์ยังไม่มี) · `isCen(code)`+`CEN_HIDE` ซ่อนแท็บ income/usage/actual/ck + show() เด้งไป exp · แดชบอร์ด ALL: multiCompare ไม่รวมส่วนกลาง + โน้ต 🏢 ค่าใช้จ่ายส่วนกลาง (ยอดยังรวมในกำไรบริษัทตามเดิม — เรื่องแบ่งเข้าสาขายังไม่เคาะ) · renderIncomeAll ตัดคอลัมน์ · ติ๊กในโมดัลสาขา (brSave ตกกลับถ้าไม่มีคอลัมน์) · smoke93
- แถบสรุปแท็บตรวจสอบเงินเป็นกล่อง pcell (ตรง/เกิน=แดง/ขาด=เหลือง/สุทธิ±/รอเข้า/เข้าธนาคาร/ปิดกะ) · sidebar จัด 5 หมวด + ซ่อนหัวหมวดตามสิทธิ์ (applyNav) · .btn เพิ่ม display:inline-block (ลิงก์ปุ่มไม่ทับข้อความ)
- จับคู่บิล QR: POS Meri (Bill Ends ≈ เวลา Beam ±2 วิ, Total collected = transaction_amount เป๊ะ) — ส.ค. รัชดาจับคู่ 4,420/4,424 · 4 บิลเงินไม่เข้า = ต้นเหตุวันแดงในกระทบยอด · ถ้าได้ POS ลาดพร้าวด้วยจะแยกยอด Beam รายสาขาได้ 100% (ยังไม่ทำเป็นฟีเจอร์ในแอพ)
- **ระวัง: มี session อื่นทำงานขนานบน main** (ครัวกลาง jjmk-kitchen.html) — ก่อน deploy ต้อง fetch origin/main แล้วเช็ค diff jjmk-pnl.html เสมอ เคย deploy ทับกัน 2 รอบ (แก้คืนแล้วทั้งคู่)
- **P&L · โหมดมือถือ (20 ก.ย.)**: เดิม `.hrow` เป็น flex แถวเดียว (แบรนด์+แถบสาขา+ช่องเดือน) ย่อไม่ได้ → หน้ากว้างขั้นต่ำ ~548px มือถือเลย **ย่อทั้งหน้า (shrink-to-fit)** ตัวหนังสือจิ๋ว แถบบนเลื่อน/กดยาก · แก้: บล็อก `@media(max-width:899px)` **ต้องอยู่ท้ายสุดของ `<style>`** (กติกา base หลายอันประกาศทีหลัง ความจำเพาะเท่ากัน = อันหลังชนะ — เคยวางไว้กลางไฟล์แล้ว .perbar/main ไม่ติด) · หัวจอเป็นกริด 2 แถว `"brand month dot" / "seg seg seg"` ทุกชิ้น `min-width:0` · `.seg`/`.tabbar` overflow-x:auto + scroll-snap + mask ขอบจาง · `header{padding-top:env(safe-area-inset-top)}` · `.perbar` 2 คอลัมน์ · `.card h3` ให้คำอธิบายลงบรรทัดใหม่ · `.twrap/.mxwrap/.tblwrap` เลื่อนในกรอบตัวเอง · `show()` เลื่อนแท็บที่เลือกมาให้เห็นเมื่อ innerWidth<900 · ตรวจด้วย `node scripts/pnl-shot.js out.png <tab>` (W=360/414/768 ได้) — ต้องได้ `innerWidth === visualViewport` ทุกจอ ไม่งั้นแปลว่ายังมีอะไรดันหน้าให้กว้าง · smoke106
- **P&L · สลิปเดียวจ่ายหลายบิล (20 ก.ย.)**: แนบสลิปจากหน้า PV (`pvSlipPick(sid,from,to,pvItemId)` → `slipPicked`) เดิมหาบิลจาก **แคชเดือนที่เปิดอยู่** (`c.exp`) และกรองเฉพาะบิลที่ยัง `!paid` → พอ PV เป็นงวดคนละเดือนกับที่เปิด หรือบิลถูกติ๊กจ่ายไปแล้ว กล่องจะว่าง ('ติ๊กจ่าย 0 บิล') · ใหม่: ถ้ามี `tgt.from` ให้ `selAllQ('pnl_expense_daily', branch+supplier_id+d ช่วงงวด)` ตรงจากฐาน · รวมบิลที่จ่ายแล้ว/มีสลิปแล้วด้วย (ติดป้าย ✓ จ่ายแล้ว / 📎 มีสลิปแล้ว) · ติ๊กมาให้เฉพาะบิลที่ยังไม่มีสลิป · ไม่มีบิลในงวด = ขึ้นข้อความบอกสาเหตุ + ปิดปุ่มยืนยัน · `assignConfirm` อัปเดตแคชตามเดือนของบิลแต่ละใบ (`S.cache[mkey(br, d.slice(0,7))]`) · smoke105
- **ระบบสั่งอาหารผ่าน QR (25 ก.ย.)** — 2 ไฟล์ใหม่ + SQL: `jjmk-order.html` (พนักงาน: PIN จาก `qr_settings.pin` ค่าตั้งต้น 1234 · แท็บ 🪑 โต๊ะ (กริดตามจำนวนโต๊ะ `qr_settings.tables` {JJRD:44,JJLP:38}) → แตะโต๊ะว่าง → เลือกแพ็กเกจ standard/premium (บังคับ) → POST `qr_sessions` {token 14 ตัว, expires_at = now+`minutes`(110)} → กล่อง QR (ไลบรารี qrcode จาก jsdelivr, ไม่มีเน็ตใช้ api.qrserver.com สำรอง) ลิงก์ = `menu_url`||`new URL('jjmk-menu.html',location)`+`?s=token` · ต่อเวลา +15 / ปิดโต๊ะ · 🧾 ออเดอร์ (โพล 5 วิ · เสียงบี๊บเมื่อ new เพิ่ม · new→cooking→served/cancelled) · 🍽 เมนู CRUD `qr_menu_items` (packages text[] · max_per_order · active) · ⚙️ จำนวนโต๊ะ/นาที/ชื่อสีแพ็กเกจ/ลิงก์/PIN upsert `qr_settings`) · `jjmk-menu.html` (ลูกค้า: `?s=token` → RPC `qr_session_info` ทุก 15 วิ (คืน session+menu ตามแพ็กเกจ+orders+now สำหรับ skew) · นาฬิกาถอยหลัง · หมวด chips · ตะกร้า → RPC `qr_place_order` (ฝั่ง DB ตรวจ open/หมดเวลา/เมนูอยู่ในแพ็กเกจ/ตัด max) · เรียกพนักงาน = order kind='call' items=[] · หมดเวลา/ปิดโต๊ะ = ซ่อนแถบสั่ง) · `sql/jjmk_qr_order.sql` (4 ตาราง RLS+policy+grant แบบโปรเจกต์ · 2 ฟังก์ชัน security definer · ทดสอบ local PG ผ่าน) · เทสต์ smoke114 (พนักงาน 24 ข้อ) / smoke115 (ลูกค้า 19 ข้อ) · ถ่ายภาพ `node scripts/qr-shot.js menu|order out.png [tables|orders|menu|settings|qr]` · **ยังไม่มี**: ราคา/บิล, ครัวแยกสถานี, จำกัดจำนวนรอบสั่ง, ภาษาอื่น, พิมพ์ QR ล่วงหน้าทั้งร้าน (ออกแบบให้สร้าง QR ต่อรอบเพราะเวลาเริ่มนับตอนสร้าง) · **กับดักเทสต์**: `S` เป็น const ต้อง `w.eval('S.cat')` · `window.scrollTo` ใน jsdom พ่น error → ใช้ `documentElement.scrollTop=0` · จำ '__orders' เป็นหมวดพิเศษต้องไม่โดน reset ใน render()
- **เครื่องจำลองปฏิทินสั่งของ (24 ก.ย.)**: `sql/jjmk_export_sim.sql` (อ่านอย่างเดียว) ดึง 1 แถว/สินค้า ทั้ง 2 สาขา พร้อมกติกาซัพ (order_mode/schedule/lead_days/order_ahead/prepay/line_group_id/cutoff/min_cases/cycle_days — ใช้ `to_jsonb(s)->>` กันคอลัมน์ที่ยังไม่มี) → `scripts/sim-orders.js snapshot.csv YYYY-MM-DD N out.json` โหลด **เอนจินจริง** ใน jsdom (สาขาละ 1 instance ตั้ง `jjsc_br`) mock stock_counts = 0 ทุกตัว ไล่ `#cd` ทีละวัน อ่าน `orderPlan()` → `scripts/sim_excel.py sim.json out.xlsx [--label] [--verify v.json]` ออก Excel 8 ชีต (กติกาซัพ+ข้อสังเกต · ซัพ×สาขา · ปฏิทินรายสาขา · เช็คสต๊อก · รายละเอียด · ผลตรวจอัตโนมัติ) · `scripts/sim_snapshot_from_export.py` แปลง CSV เก่าจาก jjmk_export_products.sql (แปลงข้อความ 'สั่งจันทร์ → ส่งพุธ' กลับเป็น schedule JSON; ไม่มี order_ahead/prepay/line_group → HAS_LG=false ปิดคำเตือนกลุ่มไลน์) · ปฏิทินที่ใช้ทดสอบ: พ.ย. 2569 (1 พ.ย. = อาทิตย์) · **ข้อค้นพบ**: ซัพที่ตั้ง 'สั่งได้ทุกวัน' + `cycle_days`/`min_cases` ของระบบเดิม (เช่น เซ็นซอรี่ รอบ 15 วัน ขั้นต่ำ 10 ลัง) หน้าสั่งของใหม่ยังไม่ใช้ค่านี้ → ขึ้นสั่งทุกวัน · order_ahead ทำให้รายการโผล่ทั้งวันล่วงหน้าและวันจริง (วันจริงพึ่ง in-transit จาก stock_receipts ถ้าวันล่วงหน้ากดส่งจริง) · ตรวจอิสระด้วย Workflow (verify→refute ต่อซัพ + critic) บนสแนปช็อตสังเคราะห์ครบทุกแบบกติกา: **เอนจินตรงกับกติกาทุกวันทุกช่อง 9/9 แบบ** (แบบที่ 10 "ซัพนอกตาราง" ตัวจำลองเคยปั้นกติกาให้เอง → แก้ให้สร้าง `sups` จาก `sup_name` เท่านั้น แล้วตรวจใหม่กับข้อมูลสดผ่าน) · **ข้อมูลสด 24 ก.ย.** (user วางตาราง markdown ในแชท → ดึงจาก transcript jsonl มาแปลงเป็น CSV): 412 สินค้า 36 ซัพ · `order_mode` มีค่าที่ 3 = **`interval`** (เซ็นซอรี่/เดสทินี/รักษ์ข้าว — ระบบเดิม) ซึ่ง `supModeOf` คืน 'interval' → เข้าทาง any (ส่งอีก lead วัน, ขึ้นสั่งทุกวัน) — ต้องเคาะกับ user ว่าจะรองรับ interval หรือให้ตั้งเป็น fixed · ซัพ `any` บางรายยังมี schedule ค้าง (สี่มุมเมือง, ครัวกลาง) ระบบไม่ใช้ · schedule ที่สองวันสั่งส่งวันเดียวกัน (สมายมีท sat+sun→mon · อินโดกูนา wed+thu→fri) ทำให้ใบเดิมขึ้น 2 วัน · order_ahead=0 / prepay=false ทุกซัพ (ยังไม่ได้ตั้ง) · ผลลัพธ์ `ทดสอบปฏิทินสั่งของ-1-15พย-ข้อมูลสด24กย.xlsx` ส่งในแชทแล้ว
- **เช็คสต๊อก · ตั้งหน่วยนับจากหน้า Safety (24 ก.ย.)**: เพิ่มคอลัมน์ "หน่วยนับ" ใน `.hd2/.row2` (กริดเป็น 7 คอลัมน์ · หน้าอื่นที่ใช้ `.hd2` override ด้วย inline style อยู่แล้วจึงไม่กระทบ) · `unitOpts(cur)` = รายการจาก `unitAll()` + หน่วยปัจจุบัน + `➕ หน่วยใหม่…` · `unitPick(id,v,el)` **เซฟทันที ไม่ผ่าน dirty/ปุ่มบันทึก** เพราะต้องรักษากติกาเดิม: หน่วยนับใช้ชื่อเดียวทั้ง 2 สาขา → `PATCH products?name=eq.` + `PATCH pnl_stock_map?product_name=eq.` (ลอกจาก `convSave` หน้า 📐) · ถาม `cfm()` ก่อนเสมอ + เตือนว่าตัวเลขที่กรอกไว้ไม่ได้แปลงหน่วยให้ · หน่วยใหม่เรียก `unitAdopt` เพิ่มเข้า `sc_units` ด้วย · **ปุ่ม 🗑 `delItemSet(id)`** = soft delete (`deleted_at`) เฉพาะแถวของสาขานี้ (ใช้ id ไม่ใช่ name) ถาม `cfm()` ก่อน + บอกว่าอีกสาขาไม่กระทบถ้ามี 2 สาขา · ลบทุกสาขาพร้อมกันยังอยู่ที่หน้า 📦 รายการสินค้า (`delProdAll`) · กริด `.hd2/.row2` เป็น 8 คอลัมน์ ต้องบีบขนาด (44/120/112/92/76×3/32 gap 7) ไม่งั้นปุ่ม 🗑 ล้นกรอบการ์ดที่จอ ~1000px · smoke113
- **บั๊ก: พิมพ์อัตราใช้แล้วเด้งกลับเป็นค่าเดิม (24 ก.ย.)**: ช่อง `ri` ในหน้า Safety วาด `value=fq(it[f])` (ค่าที่เซฟแล้ว) ทั้งที่ `setF` เก็บลง `it.dirty[f]` แล้วสั่ง `render()` → หน้าวาดใหม่ทับเลขที่เพิ่งพิมพ์ (ค่ายังอยู่ใน dirty กดบันทึกก็ได้ค่าถูก แต่บนจอเหมือนพิมพ์ไม่ติด — user เจอตอนพิมพ์ 3.5 แล้วเห็น 0.13 ค่าเดิมกลับมา) · แก้เป็น `fq(it.dirty[f]!==undefined?it.dirty[f]:it[f])` แบบเดียวกับ dropdown แผนกที่ทำถูกอยู่แล้ว · **บั๊กนี้มีมาก่อนฟีเจอร์ Enter เลื่อนช่อง** แค่เห็นชัดขึ้นเพราะกรอกเร็วขึ้น · smoke112
- **เช็คสต๊อก · คีย์บอร์ดในช่องอัตราใช้ (24 ก.ย.)**: ช่อง จ–พฤ/ศ/ส–อา ในหน้า Safety ติดคลาส `ri rk` · `onfocus=riSel(this)` เลือกข้อความทั้งช่อง (เรียก `select()` ซ้ำใน `setTimeout` เพราะ iOS ล้างการเลือกทิ้ง) · `onkeydown=riKey(event,this)` Enter = ไปช่องถัดไปตามลำดับ DOM (ขวา → ครบ 3 ช่องลงแถวถัดไป → ข้ามการ์ดแผนกได้) · **กับดัก**: `onchange` เรียก `setF` → `render()` วาดหน้าใหม่ ตัวอ้างอิง element หลุด → เลยจำเป็น **ลำดับที่** ไว้ใน `RI_NEXT` แล้ว `blur()` (ให้ change ยิงก่อน) จากนั้น `setTimeout(riFocus,0)` โฟกัสด้วย index ใหม่ · smoke112
- **เช็คสต๊อก · ตัวกรองหน้า 🛟 Safety (24 ก.ย.)**: แถบบนสุดของหน้า = เลือกแผนก · เลือกซัพ · ช่องค้นหาชื่อสินค้า/ชื่อบิล + ตัวนับ "แสดง N/M รายการ" + ปุ่ม ✕ ล้างตัวกรอง (โชว์เมื่อมีตัวกรอง) · state `S.setDept/S.setSup/S.setQ` (`setFilDept/setFilSup/setFilQ/setFilClear`) · ตัวเลือกมีจำนวนกำกับ เรียง ก–ฮ (`ยังไม่จัดแผนก` / `(ไม่ระบุซัพ)` ท้ายสุด) · ช่องค้นหาคืนโฟกัสเองหลัง render (`S._setQfocus`) · `loadAll` ล้างตัวกรองให้ (กันเปลี่ยนสาขาแล้วหน้าว่าง) · หมายเหตุ: หน้านี้ไม่มีแถบค้นหาหลัก `#deptbar` (โชว์เฉพาะแท็บนับสต๊อก) เลยต้องมีช่องค้นหาของตัวเอง · smoke111
- **เช็คสต๊อก · หน้า 🛟 Safety เรียงใหม่ (24 ก.ย.)**: การ์ด = แผนก (เรียง ก–ฮ · `ยังไม่จัดแผนก` ท้ายสุด) → ในการ์ดเรียงด้วย `cmpSupName` = ซัพก่อน (ไม่ระบุซัพท้ายสุด) แล้วชื่อสินค้าด้วย `cmpNameTH` (localeCompare 'th' ล้วน ไม่ใช้กติกา "มีครบ 2 สาขาก่อน" แบบ `cmpItem`) · **ชื่อซัพอยู่ใต้ชื่อสินค้าในแถวเหมือนเดิม — user ปฏิเสธการคั่นหัวกลุ่มซัพ (`.subh` ลองแล้วถอดออก) อย่าทำซ้ำ** · smoke110
- **อัตราใช้ต่อวันจากบิลจริง (23 ก.ย.)**: `scripts/rates_from_pnl.py` + `scripts/rates_template.sql` — กินไฟล์ export "รายสินค้า" ของ P&L หลายเดือน (แท็บ `รายสินค้า` + `สรุป`) แล้วออกเป็น SQL อัปเดต `products.rate_wk/rate_fri/rate_we` รายสาขา · วิธีคิด: ปริมาณซื้อทุกเดือนรวมกัน เกลี่ยตามยอดขายของกลุ่มวัน ÷ จำนวนวันขายของกลุ่มนั้น (คอลัมน์ `จ–พฤ/ศ/ส–อา ใช้/วัน` ในไฟล์ × จำนวนวันของเดือนนั้น แล้วหารผลรวม) · ตัวเลขอยู่ใน **หน่วยที่ลงบิล** → SQL แปลงเป็นหน่วยนับเอง (`pnl_unit_conv` → `pnl_stock_names.factor` → `products.pack`; เทียบหน่วยแบบชื่อพ้อง กก./กิโลกรัม/โล) · สินค้าที่มีหน่วยไหนแปลงไม่ได้ = **ข้ามทั้งตัว ไม่เดา** แล้วลิสต์ท้ายไฟล์ · สำรองค่าเดิมลง `jjsc_rate_backup` (batch ต่อรอบ) ย้อนกลับได้ · รันซ้ำได้: ตั้งตัวคูณเพิ่มแล้วรันใหม่ = ใส่ให้เพิ่มเอง · รอบแรก JJRD ส.ค.+ก.ย. 2569: ใส่ได้ 93 · รอตัวคูณ 77 · ไม่มีบิล 35 (คงค่าเดิม) · บิลยังไม่ผูกชื่อนับ 57 · ทดสอบกับ PostgreSQL local (seed จาก CSV ของ jjmk_export_products.sql) รัน 2 รอบ รอบสองเขียน 0 แถว
- **เช็คสต๊อก · ลากหน้าลง = รีเฟรช (21 ก.ย.)**: ของเดิมเป็นป้าย `#ptr` + `loadAll()` เฉย ๆ ไม่ได้ `preventDefault` → บนมือถือ Chrome **เบราว์เซอร์รีโหลดทั้งหน้าเอง** (ช้า/กระพริบ) · ใหม่: ยกตัวลากลงของหน้า P&L มาใช้ — `PTR{on,y0,pull,lock}` + `ptrStart/ptrMove/ptrEnd/ptrInside/ptrRender` · วงกลม ⟳ ไหลตามนิ้ว (หนืดครึ่งหนึ่ง สูงสุด 120) เกิน 70 = เปลี่ยนสี "ปล่อยเพื่อรีเฟรช" ปล่อยแล้วหมุนระหว่างโหลด · `touchmove` เป็น `passive:false` + `preventDefault` และ `html,body{overscroll-behavior-y:contain}` = เบราว์เซอร์ไม่รีโหลดเอง · ก่อนโหลดเรียก `draftSave()` (ค่านับที่ยังไม่กดบันทึกไม่หาย — `loadAll` เรียก `draftApply` คืนให้) · ไม่แย่ง gesture เมื่อ: หน้าไม่ได้อยู่บนสุด (`scrollY>2`) · `#ovl.on` (กล่องยืนยัน) · `#loginOv` เปิด · นิ้วอยู่ในกล่องที่ `scrollTop>0` · ทำงานเฉพาะจอสัมผัส (`'ontouchstart' in window` — เทสต์ต้องตั้ง `w.ontouchstart=null`) · smoke109 (smoke95 ปรับตามเกณฑ์ใหม่: นิ้วต้องลาก ~140px เพราะ pull = dy×0.5)
- **เช็คสต๊อก · เช็คหน้าจอมือถือ**: `node scripts/stk-shot.js out.png <tab>` (แท็บ dash/count/order/link/set/sched/items/cfgu/cfgd/cfgn/cfgl · `W=`/`H=` เปลี่ยนขนาดจอ · arg 4 = `full` ถ่ายทั้งหน้า) — พิมพ์ scrollWidth/innerWidth + กล่องที่กว้างเกินจอ · ตรวจ 21 ก.ย. 2569 ทุกแท็บที่ 390px: ไม่มีล้นแนวนอน (แถบแท็บบนหัวจอเลื่อนแนวนอนได้ตามดีไซน์)
- **เช็คสต๊อก · สูตรสั่งของใหม่ "ครอบคลุมถึงก่อนของชุดถัดไปมา" (21 ก.ย.)**: ของเดิมคิด `ต้องสั่ง = อัตราใช้ของวันที่ของมาส่ง − ยอดนับได้` → **สั่งขาดทุกครั้งที่ของชุดนี้ต้องใช้ข้ามวัน** (เช่น CPF สั่งศุกร์ ของมาเสาร์ ชุดหน้ามาจันทร์ → ของเสาร์ต้องกินถึงอาทิตย์ด้วย) · ใหม่: `cov=sumRate(it, วันของมา, วันที่ชุดหน้ามา)` (ไม่รวมวันชุดหน้ามา) · `pre=sumRate(it, พรุ่งนี้, วันของมา)` = ใช้ระหว่างรอ · `onWay=inTransit(...)` = ของที่สั่งไปแล้วและจะมาถึงก่อนชุดนี้ (อ่าน `stock_receipts` 10 วันล่าสุดตอน loadAll → `S.recv[product_id][order_date]`) · `left=max(0, นับได้ − pre + onWay)` · `order=max(0, cov − left)` · หัวซัพโชว์ "ครอบคลุม N วัน (เสาร์,อาทิตย์) — ชุดหน้ามา จันทร์" และแถวโชว์ "− ใช้ระหว่างรอ X + กำลังมา Y = เหลือ Z" · `nextDeliveryAfter()` ไล่วันสั่งถัดไปจากตาราง schedule (โหมดสั่งได้ทุกวัน = ครอบคลุม 1 วัน) · smoke107
- **เช็คสต๊อก · สั่งล่วงหน้า N วัน + ต้องจ่ายก่อนส่ง (21 ก.ย.)**: `suppliers.order_ahead` (0–6) = ขึ้นให้สั่งก่อนวันสั่งจริงกี่วัน (`deliveryDate` ไล่ k=0..ahead หาวันสั่งจริง แล้วใช้รอบส่งของวันนั้น · หัวซัพขึ้น "สั่งล่วงหน้า 1 วัน (วันสั่งจริง พุธ)") — แก้ปัญหาลุ้นเวลาตัดรอบซัพ ตามที่ user เคาะ "รอบสั่งวันพุธ ให้นับและสั่งตั้งแต่วันอังคาร" · `suppliers.prepay` = พอส่งใบสั่งเข้ากลุ่มซัพแล้ว ยิงข้อความ "💸 ต้องจ่ายก่อนของออก" เข้า**กลุ่มแอดมิน**อีก 1 ข้อความ (`prepayText/prepayAlert`) · กลุ่มแอดมินเก็บที่ `config.remind_line_group` (คีย์เดียวกับบอทเตือนรอบสั่งเดิม) ตั้งที่ ⚙️ ตั้งค่า › ซัพพลายเออร์ (กล่อง 💸 ใช้ตัวเลือกกลุ่มตัวเดียวกับผูกซัพ — `gPick('adm',...)`, `gpArg()` รองรับ index เป็นสตริง) · ยังไม่ได้ตั้งกลุ่ม = ส่งใบสั่งได้ปกติ แล้วเตือนต่อท้าย toast (`prepayAlert` คืน `'nogrp'`) · ยังไม่ได้รัน SQL (ไม่มีคอลัมน์ใหม่) = `supSave` ตัดฟิลด์ใหม่ออกแล้วบันทึกรอบสั่งให้ตามเดิม + เตือนให้ไปรัน · ต้องรัน **`sql/jjmk_stockcheck_supflags.sql`** · smoke108
- ⚠️ **ระบบนับสต๊อกเดิม `thananant/jjmk-stock`** (index.html production / beta.html) ทำโฟลว์ครบแล้ว — เช็คก่อนทำอะไรซ้ำ: `suppliers`(name=PK, order_mode any/fixed/interval/**monthdays**, lead_days, cycle_days, month_days, min_cases, cutoff, schedule, line_group_id, last_order_date/last_delivery_date/reopen_date) · `stock_receipts`(branch_id,product_id,order_date PK · ordered เขียนตอนส่ง LINE สำเร็จ · received/unit_price จากหน้ารับของ) = **แหล่งความจริงของของเข้า** · `products` มี cat_key/cat_label (หมวดหน้านับ **ห้ามแตะ**), bill_name(b49), pack/pack_unit (=หน่วยบิล/อัตราแปลง), order_step/step_unit, location, i18n, rate_wk/fri/we, safety/max · `config`(resetTime 06:00, special_days, pay_channels, remind_line_group) · `line_groups`, `supplier_items`, `supplier_map`, `supplier_bills`, `shift_close`, `daily_revenue`, `app_users/roles` · สูตรสั่งของเดิม (b46): `Σrate ช่วง cycle − max(0, stock − Σrate ช่วง lead)` + supHorizon() + orderRound() (ปัด pack/order_step, ยกเว้นหน่วยโล/กก./กรัม) · Edge Function `line-order`, `order-reminders.ts` · โคลนอ่านได้ที่ /home/user/thananant/jjmk-stock (CLAUDE.md ของ repo นั้นมีรายละเอียดครบ)
- **PV รอบจ่ายรายซัพ (18 ก.ย.)**: `pnl_suppliers.pay_cycle` ('period' ค่าตั้งต้น / 'monthly') — sql/jjmk_pnl_paycycle.sql (backfill เดาจาก payment_term ที่มีคำว่า เดือน/30 วัน) · pvPreview: งวด 1–15 ตัดซัพ monthly ออก · งวด 16–สิ้นเดือน ดึงทั้งเดือน (qFrom=วันที่ 1) แล้วคัด: monthly=ทั้งเดือน · period=เฉพาะ ≥16 · d_from/d_to ของใบยังเป็น 16–สิ้นเดือน · ป้ายในพรีวิว + ตารางรายจ่ายรวม · ตั้งค่าซัพมีช่อง 'รอบทำ PV' · smoke97
- **jjmk-stockcheck.html (ต้นแบบเช็คสต๊อก, 17 ก.ย.)**: ไฟล์เดี่ยวแยกจาก P&L — ลิสต์สินค้าต่อสาขาจาก `products` (ระบบนับ, STK_BR) + ป้าย 🧾 ชื่อบิลจาก `pnl_stock_names`→fallback `pnl_stock_map` (ข้าม product_id none:*) + "ครั้งก่อน" จาก `stock_current` · นับ/หมด → เขียน `stock_counts` (insert) + `stock_current` (upsert on_conflict=branch_id,product_id) **คอลัมน์ชุดเดียวกับแอพนับ b53** · ตัดวันตี 6 (`bizToday`) · ไม่มี SQL ใหม่ · smoke94 · ลิงก์ในเมนู P&L หมวดสต๊อก (แพตช์บน main โดยตรง) · **เมนู 7 หน้า**: 📊 แดชบอร์ด (หน้าแรก: กล่องสรุปนับแล้ว/ยังไม่นับ/ของหมด/ยังไม่บันทึก + ความคืบหน้ารายแผนก + งานค้าง + ลิงก์ใบสั่ง) / 🛒 สั่งของ (renderOrder+orderPlan: **ต้องสั่ง = rateOn(วันที่ของมาส่ง) − ยอดนับได้** · deliveryDate: mode any = cd+lead_days · fixed = schedule[วันนี้]→nextDow · แปลงเป็นหน่วยซื้อด้วย pnl_unit_conv (ceil) · ปุ่มคัดลอกใบสั่งลงไลน์ orderText (ฟอร์แมต: จริงใจหมูกระทะ / สาขา… / ชื่อซัพ / สั่งวันที่ <วันไทย พ.ศ.> / ส่งวันที่ … / เว้นบรรทัด / • รายการ / รวม N รายการ — ไม่มีอิโมจิหัวเรื่อง) · **ส่งทั้งหมด** (sendAllOpen→modal: เลือกปลายทาง 🏷️ กลุ่มจริงรายซัพ / 🧪 กลุ่มทดสอบกลุ่มเดียว (jjsc_testgrp) + ติ๊กเลือกซัพ + ปุ่มยืนยันบอกจำนวน · โหมดทดสอบส่งข้อความชุดเดียวกับของจริง (ไม่ติดป้ายอะไรเพิ่ม) และ **ไม่เขียน stock_receipts** · ซัพที่ยังไม่ผูกกลุ่มถูกข้ามพร้อมแจ้ง) · พับรายซัพ ค้นหาได้ · smoke96) / 📦 รายการสินค้า / นับ / ผูกชื่อ / Safety (อัตราใช้+แผนก dropdown) / 🚚 รอบสั่งซัพ (แยกจาก Safety, UI การ์ดรายวัน สั่งX→ส่งY) / ⚙️ ตั้งค่า (ล่างซ้าย ชิดบน uchip, แอดมิน — **แยกหน้าละหัวข้อ** tab `cfgu`/`cfgd`/`cfgn`, เมนูย่อยใต้ ⚙️ + แถบ .subtabs ในหน้าสำหรับจอเล็ก): ①👤 สิทธิ์ผู้ใช้ +ระดับ ผู้จัดการ(admin)/พนักงานทั่วไป(staff) เลือกได้ ②🗂 แผนก — **แยกรายสาขา** ตาราง `sc_depts(branch_id,name,zone,sort)` (sql/jjmk_stockcheck_depts.sql) เพิ่ม/ลบ/แก้ชื่อ/ย้ายของ/ตั้งโซน ทุกอย่าง branch-scoped (move=PATCH by id · rename/zone/del=PATCH products?branch_id=eq&dept=eq) · แผนกเปล่าก็ขึ้นแถบนับ · รายละเอียดพับ (toggleDept) + ค้นหา cfgQ ③📦 รายการสินค้า (tab `cfgi`) — รวมทุกสาขา (ensureAllProducts: `products?branch_id=in.(<ทั้ง2>)`) จัดกลุ่มตามแผนกของสาขาที่เปิดอยู่ · ป้ายบอกสาขาที่ใช้ (.brchip) กด ✕ = soft delete เฉพาะสาขา · ปุ่ม 🗑 เลิกใช้ = PATCH ตามชื่อ+deleted_at=is.null ทุกสาขา · ค้นหาชื่อนับ/ชื่อบิล/ซัพ · พับรายกลุ่ม ④📐 หน่วยซื้อ↔หน่วยนับ — **แผง 📏 คลังหน่วย (19 ก.ย.)** อยู่บนสุดของหน้า: `unitCard0()` รวมหน่วยทั้งหมด = ตาราง `sc_units(name unique,kind,sort)` (sql/jjmk_stockcheck_units.sql · seed จาก products.unit + pnl_stock_map.stock_unit + pnl_bill_items 180 วัน) ∪ หน่วยที่ใช้จริงในข้อมูล · `unitStats()` นับ cnt(หน่วยนับ ทุกสาขาจาก S.allBr)/map/bill/conv · เพิ่ม (`unitAdd`/`unitAdopt`/`unitAdoptAll`) · **แก้ชื่อ (`unitRename`) = PATCH products?unit=eq. (ทุกสาขา ไม่ระบุ branch) + pnl_stock_map.stock_unit + pnl_unit_conv.to_unit + sc_units** และถ้าหน่วยเดิมยังอยู่ในบิล จะ upsert ตัวคูณ 1:1 ให้เอง (ไม่งั้นใบสั่งขึ้น 'ยังไม่ใส่ตัวคูณ') · **ห้ามแก้ pnl_bill_items** (หน่วยในบิลเป็นของที่ซัพส่งมาจริง — กติกาที่ user เคาะแล้ว) · ลบได้เมื่อไม่มีสินค้าใช้ (`unitDel`) · **🔗 รวมหน่วยชื่อพ้อง (`unitMerge`)**: หน่วยคนละชื่อแต่ของเดียวกัน (โล/กก./กก) → `sc_units.alias_of` ชี้หน่วยหลัก · ทุกที่ที่เทียบหน่วยใช้ **`uSame()/uMain()`** (orderPlan, unitCard rowHtml, nTodo, convSave) ห้ามเทียบสตริงตรง ๆ อีก · merge = PATCH products.unit/pnl_stock_map/pnl_unit_conv.to_unit → ชื่อหลัก + ตั้ง alias_of + `fillAliasConv()` upsert `pnl_unit_conv` factor 1 ให้ทุกสินค้าที่บิลเขียนชื่อพ้อง (ฝั่ง P&L ไม่รู้จัก alias จึงต้องมีแถวนี้) · แยกคืนได้ (`unitUnmerge`) · ชื่อพ้องไม่ขึ้นเป็นแถวหลัก (`unitAll(withAlias)`) · SQL ส่วนที่ 2 ของ jjmk_stockcheck_units.sql: `alias_of` + ยุบทอด (ค→ข→ก) + check กันชี้ตัวเอง · datalist `#unitList` ใช้รายชื่อชุดเดียวกัน · smoke103 (15 ข้อ) + smoke104 (12 ข้อ รวมหน่วย) · ตารางด้านล่าง **แยกรายซัพ (พับได้ toggleSup)**: ensureBillUnits ดึง `pnl_bill_items(item,unit,d,supplier_id)` 180 วันของสาขา + `pnl_suppliers` (id→ชื่อ) + `pnl_unit_conv` → S.billSup{ซัพ:{ชื่อบิล:{หน่วย:{n,last}}}} · S.billUnits{item:{list,top}} · S.conv{'item|from_unit':{to,f}} · 1 สินค้ามีได้หลายซัพ/หลายหน่วย → แถวละ (สินค้า×หน่วยซื้อของซัพนั้น) ใส่ตัวคูณเอง → upsert `pnl_unit_conv?on_conflict=item,from_unit` (ใช้ร่วมกับ P&L) · หน่วยซื้อ=หน่วยนับ → ปิดช่องตัวคูณ · หน่วยนับแก้ได้จากแถว (products.unit ตามชื่อ=2 สาขา + pnl_stock_map.stock_unit) · กลุ่มท้าย "ยังไม่พบในบิล" ตั้งได้เฉพาะหน่วยนับ · convSave(i) อิง S._uRows ⑤🏭 ซัพพลายเออร์ (tab `cfgl`) — **จัดการซัพครบ**: เพิ่ม (supAdd→POST suppliers) · เปลี่ยนชื่อ (supRename→PATCH suppliers + PATCH products?sup=eq ทุกสาขา) · ย้ายของไปซัพอื่น (supMove) · ลบเมื่อไม่มีสินค้าใช้แล้ว (supDel→DELETE suppliers) · ผูกกลุ่มไลน์ — **ใช้ของระบบเดิมทั้งหมด**: กลุ่มซัพ = `suppliers.line_group_id` · รายชื่อกลุ่ม = `line_groups` (group_id,name,seen_at) · **สิทธิ์:** แอพเดิมใช้ Supabase Auth (role authenticated) แต่ตัวใหม่ยิง PostgREST ด้วย publishable key (role anon) → ต้องรัน `sql/jjmk_stockcheck_grants.sql` (grant select/insert/update บน suppliers, stock_receipts · select บน line_groups · + policy ถ้าเปิด RLS · ไม่ให้สิทธิ์ลบ) ไม่งั้นเจอ 42501 · จับคู่ชื่อซัพด้วย `supKey()` (trim+lowercase+ยุบช่องว่าง) ไม่ใช่เทียบตรง ๆ · ห้าม insert ซัพใหม่เอง (ชื่อซัพเป็น PK ของระบบเดิม) · **Edge Functions ของระบบเดิม (Deno, มี secret SUPABASE_SERVICE_ROLE_KEY + LINE_TOKEN):** `line-order` (push ข้อความเข้ากลุ่ม body {to,text}) · `sync-line-groups` (ดึง/อัปเดตชื่อกลุ่มจาก LINE → ตาราง line_groups, body {} คืน {updated}) · `order-reminders` (pg_cron ทุก 15 นาที เตือนก่อน cutoff — ไฟล์ order-reminders.ts ใน repo เดิม) · `admin-reset-password` · หมายเหตุ: Edge Function ใช้ service_role ข้าม RLS แต่ **ไม่ข้าม GRANT** · กลุ่มใหม่เข้าระบบได้ด้วยการเชิญบอท+พิมพ์ 1 ข้อความ (webhook บันทึก group_id) แล้วกด 🔄 ในแอพ · ส่งจริง = Supabase Edge Function `/functions/v1/line-order` body `{to,text}` + apikey/Bearer anon (โทเคนบอทอยู่ฝั่งเซิร์ฟเวอร์) · ส่งสำเร็จ → upsert `stock_receipts` (branch_id,product_id,order_date + ordered,sup) เหมือนแอพนับเดิม ไม่งั้นหน้ารับของ/รายงานใช้ของเพี้ยน · ข้อความใบสั่งฟอร์แมตเดียวกับแอพเดิม (🛒 ออเดอร์/🏷️ซัพ/🏪สาขา/• รายการ) · ตาราง sc_line_groups+sc_config **ไม่ใช้แล้ว** (sql/jjmk_stockcheck_line_cleanup.sql) · ปุ่มในหน้าสั่งของ: 📋 คัดลอก · 📤 ส่งเข้าไลน์ (POST {to,text,supplier,branch} ไปตัวกลาง — **โทเคนบอทอยู่ฝั่งเซิร์ฟเวอร์เท่านั้น** หน้านี้ public) · ยังไม่ตั้งตัวกลาง = fallback line.me/R/share · ปุ่ม 🔔 ทดสอบส่ง · หน้าสั่งของเรียง: ตัวที่ต้องสั่งขึ้นบน แล้วตามด้วยลำดับมาตรฐาน (วันใหม่ยังไม่นับ = ไม่มีตัวไหนถูกดัน) · แบนเนอร์ 🚚 รอบสั่งวันนี้ = สรุปสั้น (ถึงรอบสั่ง N ซัพ · สั่งได้ทุกวัน M) + ปุ่มกาง (obToggle) เป็นชิปกดไปหน้าสั่งของรายซัพ (goOrder) — เดิมยัดชื่อซัพทั้งหมดเป็นข้อความยาวอ่านไม่ออก · **deptOf = dept → cat_label (รายตัว)** ห้ามทำโหมด 'ยึด dept อย่างเดียวทั้งสาขา' อีก (เคยทำแล้วพอตั้งโซนแผนกเดียว ของอีก 172 ตัวตกไปกอง 'ยังไม่จัดแผนก' หมด) · setZone เขียนแค่ products.zone + sc_depts.zone ไม่แตะ dept/cat_label · **หลายภาษา (ไทย/อังกฤษ/ลาว/พม่า)**: ปุ่ม #langBox มุมบนขวาหัวจอ — ธงชาติเป็น **SVG ฝังในไฟล์** (FLAGSVG th/gb/la/mm ไม่โหลดจากเน็ต) + ชื่อภาษาเจ้าของภาษา/อังกฤษ + ติ๊กภาษาปัจจุบัน · LANG=localStorage jjsc_lang · **ขอบเขต: โครงหน้า (TRSHELL = #sideNav, header, .foot, #loginOv, #ptr, #toast) แปลเสมอ · เนื้อหน้าแปลเฉพาะแท็บ `count` เท่านั้น** (หน้าอื่นเป็นงานผู้จัดการ — trScope()) · แปล DOM อัตโนมัติ (translateDOM: TreeWalker เก็บ text node **ที่มีตัวอักษรใดก็ได้** (trAble — ไม่ใช่เฉพาะไทย ไม่งั้นชื่อสินค้า/แผนกภาษาอังกฤษอย่าง Best Deal, Mixfresh ค้างไม่ถูกแปล) + แอตทริบิวต์ placeholder/title/alt/aria-label) ผ่าน translate.googleapis.com/translate_a/single — **แยกยิงเป็น 2 ก้อน: คำไทยใช้ sl=th · คำที่ไม่ใช่ไทยใช้ sl=auto** (ยิงรวมกันกูเกิลเดาภาษาต้นทางผิด เหลือคำไม่ถูกแปล) batch 30 บรรทัด ถ้าจำนวนบรรทัดไม่ตรงถอยไปแปลทีละอัน · **กติกา 'กดแล้วเปลี่ยนทันที' (18 ก.ย. · วัดจริงใน Chromium: สลับภาษา 1–2ms · 0 คำขอ · ไม่มีคำค้าง)**: ⓪ `translateDOM` = 2 เฟส — เฟส sync `trApplyNow()` ทาจากแคชทุกครั้ง **แม้มีรอบแปลค้างอยู่** (ห้ามให้ของที่มีแคชไปรอเน็ต) แล้วค่อยเข้าเฟส async `trFetchPass` ทีละรอบ (`_trFetching`/`_trAgain`) · ทุกรอบถือ `gen=_trGen` (เพิ่มใน trRestore) → เปลี่ยนภาษาระหว่างรอ = รอบเก่าไม่ทาทับ และป้าย 'กำลังแปล' ปิดเฉพาะของรอบตัวเอง · `TRPEND[lang]` กัน trPass/trPrewarm ยิงคำเดียวกันซ้อน · MutationObserver กรอง `[data-notr]` + characterData ที่ TRDONE ตรงกับค่าปัจจุบัน (ไม่งั้นการเขียนของตัวแปลเองวนเรียกซ้ำ) · `pend()` ข้ามโหนดที่ทาคำแปลแล้ว (ไม่งั้นส่งคำที่แปลไปแล้วกลับไปแปลอีก) · คำที่ 'แปลแล้วได้คำเดิม' (ชื่อละติน/แบรนด์) เก็บเป็น `TRSAME='='` ทั้งในเครื่องและ sc_i18n (txt=src) → ไม่ถามซ้ำ · `TRBAD` นับเฉพาะคำที่กูเกิล **ตอบแล้ว** แต่แปลไม่ได้ (เน็ตล้มไม่นับ) และล้างของภาษานั้นทุกครั้งที่กดเปลี่ยนภาษา · ก้อนที่ตอบบรรทัดไม่ตรงถูกผ่าครึ่ง **โยนกลับเข้าคิวรวม** ให้ 4 worker ช่วยกัน · เก็บแคชทีละก้อน (ปิดแอพกลางคันไม่เสียของ) · `i18nLoad` กันยิงซ้อนด้วย promise + ล้มแล้วพัก 60 วิ · แก้คำแปลใน sc_i18n (SQL) แล้วเครื่องที่มีแคชเก่า **เปลี่ยนตามเอง** (i18nLoad คืน chg>0 → trRepaint) · ① ① `trKey()` แทนตัวเลขทุกชุดด้วย 0 ก่อนแปล/ก่อนหาแคช แล้ว `trFill()` ใส่เลขจริงกลับ (นับเลข 0 ในคำแปลต้องเท่าจำนวนเลขต้นฉบับ ไม่งั้นถอยไปแปลตัวเต็มผ่าน `trWant()`) → 'นับได้ 3 โล'/'นับได้ 12 โล' ใช้คำแปลเดียว ② `trPrewarm()` แปลล่วงหน้า **ทุกภาษา** เงียบ ๆ (debounce `trPrewarmSoon` หลัง loadAll 2.5s / หลัง render 6s): คีย์จาก `trHarvest()` (DOM ที่เห็น) + `trDataKeys()` (ชื่อ/หน่วย/'0 หน่วย'/ชื่อบิล/แผนก/ซัพ ของ S.all ทุกแผนก) + `TR_STATIC` (ประโยคคงที่หน้านับ) → ภาษาปัจจุบันก่อน แล้ว en/lo/my ที่เหลือ → เก็บทั้ง localStorage และ sc_i18n ③ `setLang` = trRestore → applyTr จากแคช (sync) → ถ้าครบไม่ยิงอะไรเลย (วัดจริงใน Chromium: 2ms · 0 คำขอ) · เทสต์ปิดการแปลล่วงหน้าด้วย `w.JJSC_NOPREWARM=1` หรือเร่งด้วย `w.JJSC_PREWARM_MS=300` · แคชขึ้น v3 `jjsc_tr3_<lang>` (TRALL แยกแมปต่อภาษา) · TRBAD คีย์ `lang|ข้อความ` · smoke101 (13 ข้อ) · แคช 3 ชั้น: **localStorage (เครื่องตัวเอง) + ตาราง `sc_i18n(lang,src,txt)` = คลังคำแปลกลาง** (sql/jjmk_stockcheck_i18n.sql · grant select/insert/update ให้ anon) — trPass ทำ 4 สเต็ป: ทาคำที่จำไว้ในเครื่องทันที → `i18nLoad(lang)` ดึงคลังกลาง (ครั้งเดียวต่อภาษาต่อ session, _i18nGot) → ส่งไปแปลเฉพาะคำที่เหลือ → `i18nSave(got)` เขียนกลับคลังกลาง (ข้ามข้อความยาว >400 ตัว) · ยิงขนานทีละ 4 ก้อน + เสร็จก้อนไหนทาก้อนนั้น (onPart) จึงไม่รอทั้งหน้า · ตารางยังไม่ถูกสร้าง/ไม่มีสิทธิ์ = เงียบ ๆ ใช้แคชในเครื่องต่อ · MutationObserver ทำให้แปลตามทุกการวาดใหม่ · **ทนตัวแปลพัง**: ตอบกลับจำนวนบรรทัดไม่ตรง → ผ่าครึ่งยิงใหม่ (trChunk recursive) จนถึงทีละคำ แทนการทิ้งทั้งก้อน · คำขอล้ม → ลองซ้ำ 1 รอบ · คำที่ยังแปลไม่ได้ **ห้ามปักธง TRDONE** (ไม่งั้นค้างเป็นไทยถาวร — เคสเมนูข้าง #sideNav ค้างทั้งแถบ) ให้วนแปลใหม่อีก 4 รอบ (1.2s×n) แล้วค่อยเตือนด้วย toast · กันเรียกซ้อนด้วย _trRun/_trAgain (เดิม await อยู่ _trBusy ยังเป็น false จึงยิงซ้อนได้) · ไม่เก็บผลที่แปลแล้วได้ข้อความเดิมลงแคช + แคชขึ้นเวอร์ชัน **jjsc_tr2_<lang>** (ของเก่ามีคำแปล=ต้นฉบับค้างอยู่) · smoke99 ทดสอบเคสตัวแปลตอบพัง/ล้ม · WeakMap TRDONE/TRATTR กันแปลซ้อนทับ (เคยได้ en:en:en:) · **TRLOG/TRALOG เก็บข้อความต้นฉบับ → trRestore() คืนไทยทุกครั้งก่อนสลับภาษา** (ไม่งั้นแปลทับจากภาษาเดิม: เมนูข้าง/หัวจอค้างเป็นอังกฤษตอนสลับ en→lo · **ห้ามกลับไปใช้ hasTH เป็นตัวกรอง**) · กลับเป็นไทย = trRestore ไม่ต้อง reload · กล่อง confirm/prompt ใช้ `cfm()/ask()` → TT() หยิบคำแปลจากแคช (ถ้ายังไม่มีก็ดึงเก็บไว้ให้ครั้งหน้า) · ฟอนต์ Noto Sans Lao / Noto Sans Myanmar + `html[lang=lo|my] *` กันตัวอักษรเป็นกล่อง · ใส่ data-notr เพื่อยกเว้น · smoke98 (19 ข้อ) + smoke99 (6 ข้อ) + smoke100 (10 ข้อ คลังคำแปลกลาง) + smoke101 (13 ข้อ แปลล่วงหน้า/ตัวเลข/ทันที) · **โหมดมือถือ (≤820px)**: หัวกระชับ (safe-area, โลโก้เล็ก, ctl แถวเดียวเลื่อนได้) · แถบเมนูบนเป็นชิปแถวเดียวเลื่อนได้ ไอคอน+ชื่อสั้น (markNav scrollIntoView) · ≤640px: การ์ดนับเต็มแถว stepper ใหญ่ · `#list .lrow` บังคับ 1 คอลัมน์ + ป้าย .lbl::before (กัน inline grid ล้นจอ) · ใบสั่ง .ordrow เป็น flex ชื่อบน/ตัวเลขล่าง · ปุ่มหัวการ์ดใช้ .hbtns (wrap ได้) · ทดสอบภาพจริงด้วย `node scripts/mobile-shot.js out.png '#tab'` (playwright + headless_shell, mock Supabase) · **แจ้งเตือนกระพริบแดง**: pill แผนกที่ยังนับไม่ครบ (.pill.needc) · การ์ดสินค้าที่ยังไม่นับ (.pcard.needc) · ซัพที่ยังไม่ผูกกลุ่มไลน์ (select.needlink) — ทั้งหมดปิดแอนิเมชันเมื่อ prefers-reduced-motion · กลุ่มไลน์: **ตัวเลือกกลุ่มแบบค้นหาได้เอง (ไม่ใช่ `<select>` แล้ว — `gPick/gpRows/gpOpen/gpSearch/gpPick`, `lineSave(i,gid)`)** เพราะ `<option>` ระบายสีเฉพาะบางช่วงไม่ได้ · ปุ่ม `.gpbtn` (ยังไม่ผูก = `.needlink` กระพริบแดง) → กล่อง `.gpmenu` มีช่องค้นหา `#gpQ` (กรองด้วย gpSearch ไม่วาดทั้งหน้า) · แถว `.gpitem` = `<span class=gpn>ชื่อกลุ่ม</span>` + ป้าย `.gpu` '· ผูกอยู่ N ซัพ' (**แดงเฉพาะป้าย**) หรือ `.gpf` '· ยังไม่ผูก' (**น้ำเงิน**) · การ์ดนี้ต้องมี class `ovok` (`.card{overflow:hidden}` จะตัดเมนูทิ้ง) · เรียงอังกฤษก่อนไทย · ชื่อกลุ่มที่ LINE ไม่ส่งมา ตั้งเองได้ (lgRename→PATCH line_groups.name, ต้องมี grant UPDATE) · **ชื่อกลุ่มเปลี่ยนในไลน์ → ตามให้เอง**: `lgAutoSync()` เรียกตอนเปิดหน้า cfgl (throttle 6 ชม. ด้วย localStorage `jjsc_lgsync`) → `syncLineGroups(true)` เงียบ ๆ แล้วเทียบชื่อเก่า/ใหม่ → `S.lgChanged` ขึ้นแถบเขียวบอกว่าเปลี่ยนจากอะไรเป็นอะไร (ปิดด้วย lgChgClear) · **ชื่อใหม่ที่ว่างจะไม่ทับของเดิม** (กันชื่อที่ตั้งเองหาย) · การผูกซัพใช้ group_id จึงไม่หลุดเวลาเปลี่ยนชื่อ · ปิดออโต้ในเทสต์ด้วย `w.JJSC_NOLGSYNC=1` หรือเซ็ต jjsc_lgsync เป็นเวลาปัจจุบัน · **ลำดับรายการมาตรฐาน** `cmpItem/cmpName` ใช้ทุกเมนู (นับ/สั่งของ/ผูกชื่อ/Safety/แผนก/รายการสินค้า/หน่วย): สินค้าที่มีครบทุกสาขาก่อน (เรียงชื่อไทย) · ที่มีบางสาขาไปท้ายกลุ่ม — อาศัย `S.nameBrN` จาก ensureAllProducts ที่โหลดทุกสาขาใน loadAll · การ์ดหน้านับติดป้าย "เฉพาะสาขา…" · **กติกา:** แผนก/ของในแผนก แยกสาขา · ที่ใช้ร่วม 2 สาขา = ชื่อบิล↔ชื่อนับ, หน่วยบิล↔หน่วยนับ · **จำสถานะรีเฟรช**: jjsc_tab/jjsc_dept + ร่างนับ jjsc_draft_<br>_<cd> (draftSave ทุก bump/setQ/setOut, draftApply หลัง prefill, ล้างวันเก่า+หลังเซฟ) · ลากลงรีเฟรช (initPtr, >75px, #ptr) · smoke95 (44 ข้อ)
- **เช็คสต๊อก v2 (17 ก.ย.)**: desktop-first + ยุบมือถือ (CSS grid) · โชว์เฉพาะที่ผูกชื่อบิล (ที่เหลือบอกจำนวนที่ซ่อน) · ชื่อบิล=ชื่อจริงตัวหลัก ชื่อนับตัวรอง · จัดกลุ่มตาม **แผนก** (`products.dept` — `sql/jjmk_stockcheck_dept.sql` ยังไม่ยืนยันรัน; แอพ degrade ใช้หมวดเดิม) แผนก/รูปอัปเดตตามชื่อ (2 สาขา) · แท็บ ⚙️ ตั้งอัตราใช้ จ–พฤ(rate_wk)/ศ(rate_fri)/ส–อา(rate_we)+safety/max **รายสาขา ลงคอลัมน์เดิมของแอพนับ** → โฟลว์คำนวณสั่ง+LINE ซัพของแอพนับใช้ต่อได้เลย · รูปอัปโหลด bucket product-images (ย่อ 640px กัน egress) · นับต่ำกว่า safety ขึ้น ⚠ · บันทึกนับใช้แผนกเป็น cat_label
- **เช็คสต๊อก v3 (17 ก.ย.)**: 3 หน้าตามแบบแอพนับ — 📋 นับ (แถบ pill แผนกบน + ค้นหาข้ามแผนก + การ์ดนับมี stepper −/+, ปุ่มหมด, ✓/⚠ สถานะ, pill ขึ้น ✓ เมื่อนับครบ) · 🔗 ผูกชื่อ (ชื่อจริง↔ชื่อนับ รายสาขา แยกกลุ่มผูกแล้ว/ยังไม่ผูก รวมตัวที่ถูกซ่อนจากหน้านับ) · 🛟 Safety (อัตราใช้ จ–พฤ/ศ/ส–อา + safety/max + แผนก แยกหน้าเดี่ยว)
- **เช็คสต๊อก v4 (17 ก.ย. รอบบ่าย)**: ชื่อนับเป็นชื่อหลัก/ชื่อบิลสีฟ้า · โซน **หน้าร้าน/หลังร้าน** (`products.zone` — sql dept ตัวอัปเดต; ตั้งโซนต่อแผนกที่หน้า Safety, PATCH dept=eq) · โลโก้ (slice LOGO_B64 จาก pnl) · วันที่/สาขา/ผู้นับย้ายไป sidebar ใต้โลโก้ (placeCtl ย้าย DOM ตามขนาดจอ) · หน้า ผูกชื่อ เพิ่มตัวเช็ค **ชื่อบิลเดียวกันแต่ชื่อนับต่างกัน** (จาก S.mapRows รวม 2 สาขา) · **login sc_users** (`sql/jjmk_stockcheck_users.sql`, hash sha256 u|p|JJSC, admin/jjmk1234 seed): จำกัด branches[]/depts[] ([]=ทั้งหมด), พนักงานเห็นเฉพาะสาขา+นับเฉพาะแผนก, Safety เฉพาะแอดมิน + การ์ดจัดการผู้ใช้ · degrade: ไม่มีตาราง sc_users = โหมดเปิด
- เช็คสต๊อกเก็บตก (17 ก.ย. เย็น): ช่องวันที่/ผู้นับใน sidebar เข้าธีม + ป้ายกำกับ · **ผู้นับล็อกเป็น username ที่ล็อกอิน** (readOnly) · หน้าผูกชื่อเช็คสองทาง (ชื่อบิลเดียว→หลายชื่อนับ และ ชื่อนับเดียว→หลายชื่อบิล) · รหัสแอดมินตั้งต้น admin/jjmk1234
- เช็คสต๊อก (17 ก.ย. ค่ำ): หน้านับไม่ซ่อนของไม่ผูก (ป้าย ⚠ แทน) · Safety ตัด safety/max ออก (ค่าเก่าจากแอพนับ ไม่ลบใน DB) · **รอบสั่ง–ส่งซัพ**: ใช้ตาราง `suppliers` ร่วมกับแอพนับ (order_mode any/fixed · schedule {mon:'tue'} · lead_days) การ์ดตั้งค่าใน Safety (`supSave` PATCH/POST ตามชื่อ) + แบนเนอร์ 🚚 หน้านับบอกวันนี้รอบสั่งใคร ส่งวันไหน · **รีเซ็ตนับตี 6**: loadAll ดึง stock_counts ของวันขายที่เลือก prefill (แถวล่าสุดต่อสินค้า + `_saved` hash กันเซฟซ้ำแถวไม่เปลี่ยน) + interval ข้ามตี 6 เด้งวันใหม่เอง (ถ้าไม่ได้เลือกวันเอง `_cdManual`)
- เทสต์รวม 55 smoke
