const pptxgen = require('pptxgenjs');
const p = new pptxgen();
p.layout = 'LAYOUT_WIDE'; // 13.33 x 7.5
const RED='8E1B1B', DARK='2A2118', MUT='6B5E4F', CREAM='FBF7EF', LINE='D8CFC0';
const FT='Sarabun';
const U='/mnt/user-data/uploads/';
const D='/home/claude/docs/';
function base(s, title, kicker){
  s.background = { color:'FFFFFF' };
  if(kicker) s.addText(kicker,{x:0.6,y:0.32,w:12,h:0.4,fontFace:FT,fontSize:13,color:RED,bold:true,isTextBox:true,margin:0});
  if(title) s.addText(title,{x:0.6,y:0.62,w:12.1,h:0.85,fontFace:FT,fontSize:30,color:DARK,bold:true,isTextBox:true,margin:0});
  s.addText('JJ P&L · จริงใจหมูกระทะ',{x:0.6,y:7.08,w:6,h:0.3,fontFace:FT,fontSize:10,color:MUT,isTextBox:true,margin:0});
}
function bullets(s,items,x,y,w,fs=15,gap=8){
  s.addText(items.map((t,i)=>({text:t,options:{bullet:{code:'2022',indent:12},breakLine:i<items.length-1,paraSpaceAfter:gap}})),
    {x,y,w,h:6.9-y,fontFace:FT,fontSize:fs,color:DARK,isTextBox:true,valign:'top',margin:0});
}
function card(s,x,y,w,h,head,body,fill=CREAM,fs=13){
  s.addShape('roundRect',{x,y,w,h,fill:{color:fill},line:{color:LINE,width:1},rectRadius:0.09});
  s.addText(head,{x:x+0.18,y:y+0.12,w:w-0.36,h:0.4,fontFace:FT,fontSize:14.5,bold:true,color:RED,isTextBox:true,margin:0});
  s.addText(body,{x:x+0.18,y:y+0.5,w:w-0.36,h:h-0.62,fontFace:FT,fontSize:fs,color:DARK,isTextBox:true,valign:'top',margin:0});
}
function shot(s,path,x,y,w,h,cap){
  s.addImage({path,x,y,w,h,sizing:{type:'contain',w,h}});
  if(cap) s.addText(cap,{x,y:y+h+0.03,w,h:0.3,fontFace:FT,fontSize:10.5,color:MUT,isTextBox:true,align:'center',margin:0});
}

// 1 ปก
let s=p.addSlide(); s.background={color:RED};
s.addText('JJ P&L',{x:0.9,y:2.1,w:11.5,h:1.3,fontFace:FT,fontSize:66,bold:true,color:'FFFFFF',isTextBox:true,margin:0});
s.addText('ระบบบัญชีรายรับ–รายจ่าย และวิเคราะห์ต้นทุนวัตถุดิบ',{x:0.95,y:3.5,w:11.5,h:0.7,fontFace:FT,fontSize:26,color:'FFE9E9',isTextBox:true,margin:0});
s.addText('จริงใจหมูกระทะ · สาขารัชดา + ลาดพร้าว · สิงหาคม 2569',{x:0.95,y:4.25,w:11.5,h:0.5,fontFace:FT,fontSize:16,color:'F5C9C9',isTextBox:true,margin:0});
s.addText('ทำงานคู่กับระบบนับสต๊อก JJ StockCheck บนฐานข้อมูลเดียวกัน',{x:0.95,y:6.6,w:11.5,h:0.4,fontFace:FT,fontSize:13,color:'F5C9C9',isTextBox:true,margin:0});

// 2 ปัญหา -> คำตอบ
s=p.addSlide(); base(s,'ทำไมต้องมีระบบนี้','ที่มา');
card(s,0.6,1.7,5.9,4.9,'ก่อนมีระบบ','• บิลกระดาษกองรวม เดือนละเกือบ 200 ใบ/สาขา คีย์ Excel ย้อนหลังทีละใบ\n• ไม่รู้ต้นทุนจริงจนกว่าจะปิดเดือน — รู้ตัวอีกทีก็สายไป\n• ราคาซัพขึ้นเงียบ ๆ ไม่มีใครเห็น\n• ของหาย/ตักเกิน/เหลือทิ้ง ไม่มีตัวเลขชี้จุด\n• สองสาขาข้อมูลคนละไฟล์ เทียบกันไม่ได้\n• safety/max ในแอพนับ ตั้งมือทีละตัว ไม่ขยับตามยอดขาย','FFF3F3',14);
card(s,6.8,1.7,5.9,4.9,'หลังมีระบบ','• ถ่ายรูปบิล → OCR อ่านรายการ+ยอดให้ ตรวจแล้วบันทึก\n• เห็นต้นทุน กำไร รายวัน–รายเดือน ทุกสาขา เรียลไทม์\n• กราฟราคารายสินค้า เตือนเมื่อซัพขยับราคา\n• "ใช้จริง" จากการนับสต๊อก เทียบ "ควรใช้" จับของรั่วเป็นตัวเลข\n• เทียบสองสาขาข้างกันในคลิกเดียว\n• คำนวณ safety/max รายวัน จ–อา ให้อัตโนมัติ (β)',CREAM,14);

// 3 ภาพรวมระบบ
s=p.addSlide(); base(s,'ภาพรวมระบบนิเวศ','โครงสร้าง');
s.addImage({path:D+'d1_arch.png',x:1.0,y:1.6,w:11.3,h:5.3});

// 4 โครงสร้างเทคนิค
s=p.addSlide(); base(s,'โครงสร้างทางเทคนิค — เบา เร็ว ค่าใช้จ่าย ฿0/เดือน','โครงสร้าง');
card(s,0.6,1.7,3.9,4.9,'ตัวแอพ','ไฟล์ HTML ไฟล์เดียวต่อแอพ\nโฮสต์ฟรีบน GitHub Pages\nเปิดจากมือถือ/คอมได้ทันที ไม่ต้องติดตั้ง\nอัปเดต = วางไฟล์ทับ 1 ไฟล์');
card(s,4.7,1.7,3.9,4.9,'ฐานข้อมูล','Supabase (PostgreSQL คลาวด์)\nทุกสาขา ทุกเครื่อง ข้อมูลชุดเดียว\nเรียลไทม์ — เครื่องหนึ่งบันทึก อีกเครื่องเห็นทันที\nรองรับข้อมูลโตหลายปี (มีดัชนีความเร็วแล้ว)');
card(s,8.8,1.7,3.9,4.9,'ตัวช่วยอัจฉริยะ','OCR สแกนบิล: Cloudflare Worker + Gemini\nจำชื่อเรียกของแต่ละซัพ (alias) — สแกนครั้งต่อไปลงถูกเอง\nแปลงหน่วยอัตโนมัติ (ลัง↔โล)\nชุดทดสอบอัตโนมัติ 48 ชุดก่อนส่งทุกเวอร์ชัน');

// 5 เมนูหลัก
s=p.addSlide(); base(s,'เมนูหลักของ JJ P&L','ฟังก์ชัน');
const menus=[['แดชบอร์ด','ภาพรวมเดือน: ยอดขาย ต้นทุน กำไร กราฟเทียบเดือนก่อน'],['รายรับ','คีย์ยอดขายรายวันจาก POS แยกช่วงเช้า/ค่ำ'],['รายจ่าย','สรุปรายจ่ายรายวัน–รายซัพ จ่ายแล้ว/ค้างจ่าย'],['บันทึกบิล','ลงบิลละเอียดรายสินค้า + OCR สแกนบิล'],['รายละเอียด (VAT)','สรุปบิลตามโหมด VAT ไว้ทำภาษี ตรวจย้อนได้'],['ข้อมูลการใช้ของ','ปริมาณ–ราคา–อัตราใช้ต่อยอดขาย รายสินค้า'],['ใช้จริง (สต๊อก)','เชื่อมการนับสต๊อก คำนวณใช้จริงรายวัน + 🤖β'],['สรุปเดือน','งบกำไรขาดทุนรายเดือน'],['ใบสำคัญจ่าย','พิมพ์เอกสารประกอบการจ่ายเงิน']];
menus.forEach((m,i)=>{ const col=i%3, row=(i-col)/3;
  card(s,0.6+col*4.1,1.65+row*1.72,3.9,1.55,m[0],m[1],i%2?'FFFFFF':CREAM,12); });

// 6 บันทึกบิล + OCR
s=p.addSlide(); base(s,'บันทึกบิล — หัวใจของข้อมูล ทำได้ใน 1 นาที/ใบ','ฟีเจอร์เด่น');
shot(s,U+'1787638543604_image.png',0.6,1.6,6.0,5.2,'เลือกซัพจากชิป → ชุดสินค้าประจำขึ้นให้เอง');
bullets(s,[ 'เลือกซัพพลายเออร์ → รายการสินค้าประจำของเจ้านั้นขึ้นครบ ใส่แค่จำนวน–ราคา',
'📷 สแกนบิล: ถ่ายรูป → OCR อ่านรายการ+ยอดให้ ตรวจ 10 วิ แล้วบันทึก',
'จำชื่อบนบิลของแต่ละซัพ — สแกนซ้ำครั้งหน้า ลงถูกเองเงียบ ๆ',
'สินค้าใหม่ในบิล → เด้งถามผูกกับระบบนับสต๊อกทันที (ผูกครั้งเดียวจำตลอด)',
'บันทึกแล้วยอดไหลเข้ารายจ่าย/VAT/การใช้ของ อัตโนมัติทุกหน้า'],6.9,1.8,5.9,14.5);

// 7 รายละเอียด VAT
s=p.addSlide(); base(s,'รายละเอียดบิลตามโหมด VAT — พร้อมยื่นบัญชี','ฟีเจอร์เด่น');
shot(s,U+'IMG_6699.PNG',0.6,1.55,5.6,5.3,'สรุป ไม่มี VAT / ยังไม่รวม / รวมแล้ว + รายใบ');
bullets(s,['แยกยอด ก่อน VAT · VAT · สุทธิ ตามโหมดของแต่ละบิลอัตโนมัติ',
'ตารางรายใบครบทุกบิล แตะแถวเปิดแก้ได้ทันที',
'ใช้ประกอบการยื่นภาษีซื้อ และตรวจสอบย้อนหลังกับบิลกระดาษ',
'ดึงข้อมูลแบบแบ่งหน้า — เดือนที่บิลหลายพันแถวก็แสดงครบ'],6.5,1.9,6.3,15);

// 8 ข้อมูลการใช้ของ
s=p.addSlide(); base(s,'ข้อมูลการใช้ของ — รู้ทุกบาทที่ซื้อ','ฟีเจอร์เด่น');
shot(s,U+'1787773963730_image.png',0.6,1.55,6.4,5.3,'รายสินค้า/แยกซัพ · อัตราใช้ต่อยอดขาย ฿10,000');
bullets(s,['รวมทุกซัพเป็นรายสินค้า หรือแยกตามซัพก็ได้',
'อัตราใช้ต่อยอดขาย ฿10,000 + ใช้/วัน แยก จ–พฤ / ศ / ส–อา',
'แตะสินค้า → กราฟจำนวนซื้อ + เส้นราคาแยกรายซัพ เห็นเจ้าไหนขึ้นราคา',
'⚠ หน่วยปน: ซัพส่งลังบ้างโลบ้าง ตั้งอัตราแปลงครั้งเดียว ระบบรวมให้',
'ออกรายงาน PDF หน้า A4/สินค้า เลือกกรองตามซัพ/หมวดได้'],7.2,1.8,5.6,14);

// 9 เทียบสาขา
s=p.addSlide(); base(s,'🆚 เทียบสาขา — สาขาสอนสาขา','ฟีเจอร์เด่น');
shot(s,U+'1787775872371_image.png',0.6,1.6,7.2,5.1,'เรียงตามผลต่างที่เป็นเงินมากสุดก่อน');
bullets(s,['เทียบอัตราใช้ต่อ ฿10,000 — ตัดผลร้านขายมาก/น้อย เทียบแฟร์',
'▲▼ ต่างเกิน ±20% ทาแดง สะดุดตาทันที',
'เรียงตัวที่ต่างเป็นเงินมากสุดขึ้นก่อน — รู้ว่าต้องไปไล่ดูตัวไหน',
'ใช้หาว่าอีกสาขาทำงานยังไงถึงประหยัดกว่า'],8.0,1.9,4.8,14.5);

// 10 ใช้จริง (สต๊อก)
s=p.addSlide(); base(s,'ใช้จริง (สต๊อก) — เชื่อมการนับของเข้ากับบัญชี','ฟีเจอร์เด่น');
shot(s,U+'1787776778245_image.png',0.6,1.55,5.8,5.25,'ใช้จริงรายช่วงนับ + เทียบค่าเฉลี่ยเดือน');
s.addImage({path:D+'d3_use.png',x:6.6,y:1.7,w:6.1,h:1.5});
bullets(s,['พนักงานนับตามเดิม ระบบอ่านผลนับมาคิดให้เอง',
'ดูได้ 3 โหมด: ช่วงล่าสุด · รายวัน (ปุ่ม ‹ ›) · กำหนดช่วงวันที่เอง',
'ติดลบ = มีของเข้าที่ยังไม่มีบิล — ระบบทาแดง + ⏳ บอกสาเหตุเอง',
'ผูกชื่อสองระบบครั้งเดียวจำตลอด มีตัวเดา+ผูกตามอีกสาขาให้'],6.7,3.5,6.0,13.5);

// 11 กันพลาด
s=p.addSlide(); base(s,'ระบบกันพลาด — ออกแบบให้เลขเชื่อถือได้','คุณภาพ');
const guards=[['ป้ายแดงบนเมนู','สินค้าใหม่ในระบบนับที่ยังไม่ผูก — เตือนจนกว่าจะครบ'],['แถบ ⏳ บิลยังไม่ลง','วันนับล่าสุดไม่มีบิล → บอกเองว่าเลขชั่วคราว'],['⚠ หน่วยปน','ชื่อเดียวหลายหน่วย → ชวนตั้งอัตราแปลง ไม่มั่วรวม'],['เพดานข้อมูล','ดึงแบ่งหน้าจนครบ เดือนหมื่นแถวก็ไม่ตกหล่น'],['เรียลไทม์หลายเครื่อง','แก้พร้อมกันไม่ทับกัน มีตัวกันฟอร์มค้าง'],['ทดสอบอัตโนมัติ 48 ชุด','ทุกเวอร์ชันผ่านการไล่เช็คเลขกับมือก่อนส่ง']];
guards.forEach((m,i)=>{ const col=i%3,row=(i-col)/3; card(s,0.6+col*4.1,1.75+row*2.35,3.9,2.15,m[0],m[1],row?'FFFFFF':CREAM,12.5); });

// 12 β
s=p.addSlide(); base(s,'🤖 ตั้งค่า safety/max อัตโนมัติ (beta)','ก้าวต่อไปที่เริ่มแล้ว');
bullets(s,['ใช้ข้อมูล "ใช้จริงรายวัน" คำนวณค่าที่ควรตั้ง แยกวันจันทร์–อาทิตย์',
'สูตรปรับได้: safety = ใช้เฉลี่ย ×1.2 · max = safety ×2 · วันหยุดพิเศษ ×1.3',
'ตัวข้อมูลน้อยติดป้าย ไม่ติ๊กให้เอง — คนตรวจก่อนเสมอ',
'กดยืนยัน → เขียนเข้าแอพนับ โฟลว์สั่งของเดิมใช้เลขใหม่ทันที',
'เฟสถัดไป: ระบบเขียนเองทุกเช้า + สั่งของอัตโนมัติเต็มรูปแบบ'],0.7,1.9,7.6,16);
card(s,8.6,1.9,4.1,4.6,'ทำไมสำคัญ','เลิกตั้งตัวเลข ~200 ตัว × 2 สาขา ด้วยมือ\n\nวันศุกร์เตรียมของไม่เท่าวันจันทร์อีกต่อไป\n\nยอดขายโต เลขขยับตามเอง','FFF3F3',13.5);

// 13 ข้อดีสรุป
s=p.addSlide(); base(s,'สรุปข้อดี','คุณค่า');
const pros=[['ประหยัดเวลา','ลงบิล 1 นาที/ใบ ด้วย OCR · งานคีย์ Excel หายไปทั้งก้อน'],['เห็นเงินทุกบาท','ต้นทุน–กำไรรายวัน · กราฟราคาไว้ต่อรองซัพ'],['จับของรั่ว','ใช้จริง vs ควรใช้ ชี้เป็นตัวเลขรายสินค้า'],['สองสาขาหนึ่งจอ','เทียบกันแฟร์ ๆ เอาวิธีที่ดีไปใช้ทั้งเครือ'],['ต่อยอดอัตโนมัติ','ฐานข้อมูลพร้อมสำหรับสั่งของอัตโนมัติเต็มระบบ'],['ค่าใช้จ่าย ฿0/เดือน','โฮสต์ฟรี ฐานข้อมูลฟรี ไม่มีค่ารายเดือน']];
pros.forEach((m,i)=>{ const col=i%3,row=(i-col)/3; card(s,0.6+col*4.1,1.75+row*2.35,3.9,2.15,m[0],m[1],i%2?CREAM:'FFFFFF',12.5); });

// 14 โรดแมป
s=p.addSlide(); base(s,'โรดแมป','ต่อจากนี้');
bullets(s,['ก.ย. 2569 — เปิดใช้ 🤖 safety/max อัตโนมัติเต็มรูปแบบ (ข้อมูลครบ 4 สัปดาห์)',
'สำรองข้อมูลลง NAS ทุกคืน — สำเนาที่ร้านถือเอง',
'📱 LINE สรุปเช้าอัตโนมัติ: ยอดขาย · ของใช้ผิดปกติ · ราคาซัพขยับ · บิลค้างลง',
'สั่งของอัตโนมัติ: ระบบร่างใบสั่ง คนกดยืนยันเดียว',
'💬 JJ Copilot: พิมพ์ถามข้อมูลร้านเป็นภาษาคน'],0.7,2.0,11.8,17,10);
p.writeFile({fileName:'/home/claude/docs/JJ-PnL-Presentation.pptx'}).then(()=>console.log('pptx written'));
