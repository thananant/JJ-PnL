{
  "name": "jj-kpi-test",
  "private": true,
  "scripts": {
    "test": "TZ=Asia/Bangkok node smoke.js",
    "check": "node -e \"const s=require('fs').readFileSync('../../jjmk-kpi.html','utf8');const m=s.match(/<script>\\n('use strict';[\\s\\S]*?)<\\/script>\\s*<\\/body>/);require('fs').writeFileSync('/tmp/app.js',m[1]);\" && node --check /tmp/app.js && echo SYNTAX OK"
  },
  "dependencies": { "jsdom": "^24.1.3" }
}
